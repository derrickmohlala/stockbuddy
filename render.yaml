services:
  - type: web
    name: stockbuddy-backend
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && gunicorn app:app -b 0.0.0.0:$PORT
    envVars:
      - key: PYTHONPATH
        value: backend
      - key: DATABASE_URL
        fromDatabase:
          name: stockbuddy-db
          property: connectionString

databases:
  - name: stockbuddy-db
    plan: free
    databaseName: stockbuddy
    user: stockbuddy_user
    ipAllowList: [] # Allow all for simplicity, or specify if needed
